resources:
  schemas:
    landing:
      name: landing
      catalog_name: ${var.catalog_name}
    approach_notebooks:
      name: approach_notebooks
      catalog_name: ${var.catalog_name}
    approach_sql:
      name: approach_sql
      catalog_name: ${var.catalog_name}
    approach_dbt:
      name: approach_dbt
      catalog_name: ${var.catalog_name}
    approach_dpl_sql:
      name: approach_dpl_sql
      catalog_name: ${var.catalog_name}
    approach_dpl_python:
      name: approach_dpl_python
      catalog_name: ${var.catalog_name}
    approach_mv_st:
      name: approach_mv_st
      catalog_name: ${var.catalog_name}
    approach_dlt:
      name: approach_dlt
      catalog_name: ${var.catalog_name}

  volumes:
    raw_files:
      name: raw_files
      catalog_name: ${var.catalog_name}
      schema_name: landing
